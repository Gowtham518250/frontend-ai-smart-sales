/// Copy this file to `email_secrets.local.dart` (same folder) and paste your
/// working Gmail + App Password from your old project.
///
///   copy lib\email_secrets.local.dart.example lib\email_secrets.local.dart
///
/// Do NOT commit `email_secrets.local.dart` (it is gitignored).
/// Release builds ignore this file — use `--dart-define=EMAIL_SENDER=...` instead.
/// If credentials were ever exposed, rotate the Gmail App Password immediately.

const String emailSecretsSender = 'yourshop@gmail.com';
const String emailSecretsAppPassword = 'your-16-char-app-password';
